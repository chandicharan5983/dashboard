export interface FREQUENTLY_USED_DATA {
    title: string;
    message: string,
    actionButton: string
}

export interface NAV {
    title: string,
    url: string,
    isActive: boolean,
    isDisabled: boolean,
}